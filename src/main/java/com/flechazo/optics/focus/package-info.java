/**
 * Defines fluent paths that apply optic operations to a bound source value.
 *
 * <p>Each path operation returns a new path or observation and leaves the bound source value
 * unchanged.
 */
@NullMarked
package com.flechazo.optics.focus;

import org.jspecify.annotations.NullMarked;
